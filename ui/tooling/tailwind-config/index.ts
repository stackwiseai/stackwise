import type { Config } from "tailwindcss";

const config: Config = {
  content: [""],
  theme: {
    extend: {},
  },
  plugins: [],
  variants: {
    extend: {
      backgroundColor: ["disabled"],
    },
  },
};
export default config;
