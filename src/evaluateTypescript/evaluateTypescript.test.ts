import evaluateTypeScript from '.';
import astParser from '.';
import * as fs from 'fs';


test('flattenInputJson correctly', () => {
//   const code = `{
//     "ok": true
//   }
// `;
//   const result = evaluateTypeScript('test-workspace/fixtures/arrays/input.ts');
//     // write result into a file
//     fs.writeFileSync('test-workspace/fixtures/arrays/astParserResults.json', JSON.stringify(result, null, 2));
  });
