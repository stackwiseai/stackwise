briefs:
add a way to send a text and get back a text

---------
CODE HELP
import OpenAI from 'openai';

const openai = new OpenAI({
});

async function main() {
  const chatCompletion = await openai.chat.completions.create({
    messages: [{ role: 'user', content: 'Say this is a test' }],
    model: 'gpt-4-turbo',
  });
}

main();
---------

now please query openai and gpt-4 using this form, the output should be the response from gpt-4