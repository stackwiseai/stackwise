briefs:
add a way to send a text and get back a text

---------
PLEASE TAKE INTO ACCOUNT THE FOLLOWING CODE before answering the question:
import OpenAI from 'openai';

const openai = new OpenAI({
});

async function main() {
  const chatCompletion = await openai.chat.completions.create({
    messages: [{ role: 'user', content: 'Say this is a test' }],
    model: 'gpt-4-turbo',
  });
}

main();
---------
now this form should query openai using the code below, and the response of gpt-4-turbo should also be returnedPlease could you rewrite entirely this file, following the system instructions ?